# Claude CLI overlay — mounts OpenClaw's Claude auth credentials.
services:
  goclaw:
    build:
      args:
        ENABLE_CLAUDE_CLI: "true"
    volumes:
      - /Users/primadonna/.openclaw/claude-auth:/app/.claude-host:ro
